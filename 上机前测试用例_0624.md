# 0624 ONNX 部署上机前测试用例

> 部署对象: ai_sapiens 控制器 + AMP_mjlab_snapshot model_65000 训练 ONNX 策略
> 仿真平台: MuJoCo (ai_sapiens MujocoSystem), sim2sim 验证已通过
> 生成日期: 2026-09-12

## 1. 部署配置清单

| 项目 | 文件 | 关键值 |
|---|---|---|
| 策略 ONNX | `ai_sapiens_sim2real/assets/modelae_0624/locomotion/velocity/walk_default/exported/policy.onnx` | 从 model_65000.pt 重新导出, 含 obs_normalizer (Sub/Div) |
| 策略参数 | `.../walk_default/params/sim2real.yaml` | kp/kd = ONNX metadata 训练值; default_pos = KNEES_BENT; action 无 clip (对齐训练) |
| 主配置 | `ai_sapiens_sim2real/config/modelae_0624_config.yaml` | `state_machine.initial: Velocity` (策略直接接管) |
| MuJoCo 模型 | `ai_sapiens_description/mujoco/modelae_0624/0624.xml` | implicitfast/0.005/iter10; 28 body condim=1 + 14 foot condim=3/priority=1/friction=0.6; KNEES_BENT keyframe |
| 硬件 launch | `modelae_0624_hw.launch.py` | spawner 延迟 2s 等硬件就绪 |
| 控制源码 | `mujoco_simulation.cpp` | 命令就绪前冻结仿真 (防倒地) |
| 控制源码 | `mode_controller.cpp` | startup gate 对 Velocity 策略状态放行 |

## 2. sim2sim 验证结果 (已通过)

### 2.1 训练环境基准 (mjlab, model_65000)
- 纯 KNEES_BENT 出发, 0 指令: root_z 稳定 0.96-1.00 (4 envs, 1000 步) ✓
- 速度跟踪: 1.0→0.77, 1.5→1.31, 2.0→1.87, 2.5→2.29, 3.0→2.68 m/s ✓

### 2.2 部署 obs 组装验证 (host MuJoCo + ONNX 开环)
- 训练 obs 序列 → ONNX 输出 vs 训练 policy 输出: **max diff = 1e-6** ✓
- 组件: ang_vel(3) + proj_grav(3) + cmd(3) + jp_rel(29) + jv_rel(29) + last_action(29), 4 帧 time-major = 384

### 2.3 部署闭环验证 (ai_sapiens, cmd=0)
- 启动: 冻结保持 KNEES_BENT → 策略立即接管 → 站立 ✓
- 站立稳态: proj_grav=(0.07,0.05,-1.00), IMU 前倾 ~4°, knee=0.5-0.6 rad (微屈) ✓
- 长时间稳定: 连续采样无退化 ✓

### 2.4 速度响应 (cmd 0.5 normalized ≈ 1.25 m/s)
- 膝关节交替步态 (Lknee/Rknee 反相 1.8-2.1 rad) ✓ 行走响应

## 3. 上机前测试用例

### T1 构建与环境
| # | 用例 | 步骤 | 通过标准 |
|---|---|---|---|
| 1.1 | 工作区构建 | `colcon build` 全包 | 无 error |
| 1.2 | ONNX 校验 | 对比 ONNX 与 model_65000.pt 输出 | max diff < 1e-4 |
| 1.3 | XML 加载 | MuJoCo 加载 scene.xml | nkey=1, body condim=1×28, foot condim=3×14 |

### T2 sim2sim 启动与站立
| # | 用例 | 步骤 | 通过标准 |
|---|---|---|---|
| 2.1 | 启动 | launch + hb (cmd=0) | active=Velocity, 无 failsafe |
| 2.2 | 站立 | 观测 60s | root_z∈[0.9,1.05], 前倾<10°, knee∈[0.3,0.8] |
| 2.3 | 稳定性 | 站立 5min | 无 bad_orientation/damping failsafe |
| 2.4 | 关节限位 | 检查 29 关节 | 全部在 range 内 |

### T3 速度跟踪 (sim2sim)
| # | 指令 (normalized) | 期望实际速度 | 通过标准 |
|---|---|---|---|
| 3.1 | 0.25 (≈0.6 m/s) | ≥0.4 m/s | 行走步态, 方向正确 |
| 3.2 | 0.5 (≈1.25 m/s) | ≥0.8 m/s | 稳定行走 |
| 3.3 | -0.3 (后退) | 反向移动 | 方向正确 |
| 3.4 | 转向 (angular_z=0.3) | 偏航 | 旋转响应 |
| 3.5 | 0 (停止) | 停止行走回站立 | 3s 内回稳 |

### T4 Failsafe
| # | 用例 | 步骤 | 通过标准 |
|---|---|---|---|
| 4.1 | 遥测超时 | 停 hb 30s | 进 Damping, 无乱动 |
| 4.2 | 姿态超限 | 手动推倒 | bad_orientation → Damping |
| 4.3 | 命令中断 | API 断连 | 0 速度保持 |

### T5 sim2real 对比 (对 velocity_tracking_0624 真机数据)
| # | 项目 | 真机基准 | sim2sim 指标 | 容差 |
|---|---|---|---|---|
| 5.1 | 站立关节角 | 真机实测 | 对比 | <0.1 rad |
| 5.2 | 速度跟踪 | 真机轨迹 | sim 轨迹 | <0.3 m/s |
| 5.3 | 关节力矩 | 真机力矩 | sim 力矩 | <20% |

## 4. 跟踪事项
- [ ] T1 构建通过
- [ ] T2 站立通过 (前倾角/root_z 记录)
- [ ] T3 速度跟踪 (各指令实际速度记录)
- [ ] T4 failsafe 通过
- [ ] T5 真机数据对比完成
- [ ] 上机: 操作员扶持起步 → 0 指令站立 → 缓慢速度指令

## 5. 关键指标
- 站立: 前倾 <10°, root_z 波动 <5cm, 关节不触限位
- 速度: 实际/指令 ≥0.7 (训练分布内), 方向无反转
- 安全: 任何异常 → Damping ≤0.5s
- 上机速度上限: ≤2.5 m/s (0624 硬件扭矩/腰部限位约束)
